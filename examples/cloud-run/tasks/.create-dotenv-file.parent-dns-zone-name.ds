# This is a duckscript file.
# https://github.com/sagiegurari/duckscript

echo
echo Please input your Parent DNS Zone Name.
echo Enter your value:
parent_dns_zone_name = read
if is_empty ${parent_dns_zone_name}
  assert_error "Please retry and input your Parent DNS Zone Name"
end
